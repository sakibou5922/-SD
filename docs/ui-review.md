# UI Finish Gate — 株式会社SD コーポレートサイト

**レビュー**: UI Finish-Gate Reviewer
**対象ビルド**: `index.html` / `src/styles/*.css` / `src/scroll/sections.ts` / `src/three/scene.ts`
**証拠**: `scratchpad/shots/1440x900-00..12.png`（デスクトップ）, `scratchpad/shots-m/390x844-00..10.png`（モバイル、1ビルド前。差分は3Dスケール0.75とカメラ距離のみ、レイアウト同一）
**注記**: ヘッドレス環境で Google Fonts が読めていないため、書体そのものは判定対象外。
**契約**: `docs/brand-brief.md`（ブランドの正）, `docs/scroll-storyboard.md`（3D・モーション・レイアウト・コントラスト要件）

---

## 0. プロダクト・レンズ（何を見るか）

- **ユーザーと仕事**: 自社サイトを「ちゃんと」つくりたい中小企業・スタートアップ・事業部門の担当者が、SDが何をどう作る会社かを理解し、「相談する」まで到達する。
- **最初に読まれるべきもの**: タグライン「構造が、動き出す。」と、その意味を体現する3D（点 → 構造 → 実体）。
- **主アクション**: `相談する`（ヒーロー／ヘッダー／Service末尾／Contact）。
- **密度**: 疎。暗い背景が主役、発光は「点と線」に限る（brief §3.1 運用ルール）。
- **禁止される既定値**: 3枚カードグリッド、番号だけのプレースホルダ、光る大面積オブジェクト、意味のないスクロール余白、読めない「非アクティブ」テキスト。

---

## 1. 判定

### HOLD

理由: ページの骨格（タグライン主導のヒーロー、1つの点群が7つの形に変わる物語、番号付きリストで語る Service / Process、装飾のない会社概要テーブル、コーラルをCTAだけに使う色の規律）は明確にこのプロダクトのものであり、書き直す必要はない。しかし、**モバイルで Service カード4枚・Process ステップが「非アクティブ状態（opacity 0.3〜0.35）」のまま画面内に並び、事業内容そのものが読めない**（`390x844-03.png`, `-05.png`）。これは storyboard §6.7 の必須要件（テキスト直下で 4.5:1）に対する明確な違反で、サイトの中核情報が欠落するのと同じである。加えて Works → Company の遷移で **真鍮色の結び目メッシュが1画面分を占め、テキストのないビューポートが約900px続く**（`1440x900-09.png`）。brief §3.1「発光色は画面全体の5%以下」と §6「副発光は10%以下」に反し、ページで最も「他社のWebGLデモ」に見える瞬間を作っている。この2件が解消され、下記 Major の About/Service の3D重なり・モバイルの空白・フッターの空洞が観測可能に直るまで出荷しない。

---

## 2. 所見（重大度順）

凡例: **C** = Critical（出荷停止）, **M** = Major（PASS前に必須）, **m** = Minor（推奨。PASSは阻まない）

| # | 重大度 | 画面 / 証拠 | 何が汎用的・破綻しているか | 観測可能な変更 | 検証条件 | 根拠 |
|---|---|---|---|---|---|---|
| 1 | **C** | モバイル Service / Process<br>`390x844-03.png`, `-04.png`, `-05.png` | ピンなしのモバイルでもカード／ステップの点灯がセクション進行（`0.3 + i×0.15` / `0.15 + i×0.17`）に scrub されるため、画面内にあるカード4枚すべて、ステップ5つのうち4つが opacity 0.35 / 0.3 のまま。見出し `#F2F4F7`@0.3 ≈ 2.4:1、説明 `#9AA3B2`@0.3 ≈ 1.5:1。事業内容と工程が読めない。 | `sections.ts`: `pinService / pinProcess` が false のとき、`.card` / `.step` の opacity tween を timeline に置かない。代わりに要素ごとの reveal（`start: 'top 80%'`, `toggleActions: 'play none none reverse'`）で opacity 1 に入場させ、`--bar` の点灯だけをセクション進行に残す。CSS の初期値 `.card{opacity:.35}` `.step{opacity:.3}` は `@media (min-width:1024px)` に限定する。 | 390×844 でセクションを100pxずつスクロールし、上端がビューポート80%より上にあるカード／ステップは常に opacity 1。`-03.png` 相当のフレームで4枚とも白文字。合成コントラスト ≥ 4.5:1。 | storyboard §5.2「各要素の入場は個別トリガー」, §6.7 受け入れ基準 |
| 2 | **C** | Works → Company 遷移（デスクトップ）<br>`1440x900-09.png` | Works local 0.85 まで `knotOpacity: 1`、カメラ z 5.2 で結び目メッシュが幅の約60%を真鍮色で占め、その間 DOM テキストがゼロ（Works 150vh + Company 120vh、いずれも中央寄せで上下に約450pxずつ空く）。brief の「暗さが主役」「副発光は節点だけ」に反し、ページ内で唯一「汎用WebGLショーリール」の絵になる。モバイルでも同様（`390x844-07.png` 下半分）。 | (a) `sections.ts` Works: `knotOpacity` の到達値を 1 → **0.55**、フェードアウトを local **0.6–0.8** に前倒し（カードが画面内にある間に実体が現れて消える）。(b) Works のカメラ `camZ: 5.2` → **6.2**（実体がカード列の背後に収まる）。(c) `sections.css`: `.section--works{min-height:120vh}`, `.section--company{min-height:100vh; align-items:start; padding-top:22vh}`。 | 1440×900 で Works local 0.5 のフレーム: 3枚のカードが全て見え、真鍮色（`#D4A65A` 系）のピクセル面積が画面の 10% 以下。Works カード下端から Company テーブル1行目までのスクロール中、テキストが一切ないビューポートが **50vh（450px）を超えない**。 | brief §3.1 運用ルール（5%）, §6-2（10%）, §6-4「密度は疎」; storyboard §4-2「テキストが読まれる区間で形が安定」 |
| 3 | **M** | Service / Process ピン（デスクトップ）<br>`1440x900-03.png`, `-06.png`, `-07.png` | ピン開始フレーム（`-03`）でカード4枚が全て 0.35 の灰色。非アクティブ opacity 0.3 は見出しで約 2.4:1、説明文で約 1.5:1 と、大きな文字の 3:1 にも届かない。「1つだけ光る」意図は正しいが、storyboard 自身の §6.7 と矛盾しており、§6.7（受け入れ基準）が優先。 | 非アクティブを **opacity 0.6** に（見出し `#F2F4F7`@0.6 ≈ 6.7:1、説明文 ≈ 3.4:1 で「次に読むもの」として許容）。Service はカード1の点灯位置を `s = 0.3` → **0.05** にし、ピン開始時に必ず1枚が読める状態にする。Process も同様に step 01 を `0.15` → `0.05`。 | 1440×900 で Service local 0.02 / Process local 0.02 のスクリーンショットに、白文字の項目が1つ以上ある。非アクティブ項目の見出しが合成で ≥ 4.5:1。 | storyboard §6.7, §3.3 / §3.4（意図は維持） |
| 4 | **M** | About（デスクトップ）<br>`1440x900-01.png` | 本文3段落（右 col 8–12）が球体の点群の真上に置かれ、スクリムは左（`scrim-l`）にしかない。見出し側は空、本文側だけ3Dと競合しており、storyboard §6.7「テキスト領域の実効背景を `#2A2F3A` 以下」が満たせない。 | `index.html` About を `scrim-l` → **`scrim-r`** に変更し、`sections.ts` の `X_ABOUT` を `-0.9` → **`+0.3`**（球の中心が幅の約40%位置、見出しの下の空き領域に来る）。 | About local 0.5 で本文カラム（x 832–1300px）の直下に点群が入らない。DevTools 合成スクリーンショットで本文 ≥ 4.5:1。 | storyboard §6.7, §3.2（右5列に本文） |
| 5 | **M** | Service / Process ピン（デスクトップ）<br>`1440x900-03.png`, `-04.png`, `-06.png` | 格子の左端（x≈600）がカード説明文の右端（x≈705）に約100px食い込む（「設計します」「実装します」の上に線が走る）。Process でも設計図の点が x 560–600 でステップ列に掛かる。 | `sections.ts`: `X_SERVICE: -1.3` → **`-1.9`**, `X_PROCESS: -1.1` → **`-1.5`**。または `.card__body, .step__body { max-width: 30em }` でテキストを col-5 相当に収める（storyboard §3.3「テキストは左50%」）。 | 1440×900 で Service local 0.5 / Process local 0.5 のフレームで、3Dのピクセルが x < 720px のテキスト行ボックスと交差しない。 | storyboard §3.3, §3.4, §6.1「テキストブロックは6列以内、3Dの可視領域を残す」 |
| 6 | **M** | モバイル セクション余白<br>`390x844-02.png`（下 450px 空）, `-05.png` 下部, `-08.png`（上 300px 空） | About 150vh / Works 150vh / Company 120vh がモバイルにもそのまま適用され、中央寄せの結果、セクション間に 300–450px のテキストなし領域が連続する。3Dは画面上部に小さく残るだけで、空白に意味がない。 | `sections.css` に `@media (max-width:767px){ .section--about, .section--works, .section--company, .section--contact { min-height:auto; padding: var(--space-16) 0; } }`。3D遷移は section 高さに紐づくため短くなるが、モバイルは簡略カメラで良い（storyboard §5.2）。 | 390×844 で Hero 〜 Footer を 200px 刻みでスクロールし、#2 で許容した Works→Company の1箇所を除き、テキストが一切ないフレームがない。 | storyboard §5.2, §3.0（高さは「目安」であり演出のためのもの） |
| 7 | **M** | Contact → Footer（両サイズ）<br>`1440x900-11.png`, `390x844-10.png` | `.footer{min-height:50vh; align-items:center}` により、補足文からフッター罫線まで約250px、罫線からフッター内容まで約200px、内容の下に約180px の空洞。ページの最後が「余韻」ではなく「切れた」印象で終わる。storyboard §6.5-5 のメールアドレス直書きも無い。 | `components.css`: `.footer{min-height:0; padding: var(--space-12) 0 calc(var(--space-8) + env(safe-area-inset-bottom));}` `.footer__inner` の gap を `var(--space-6)` に。`footer__brand` の下に `<a href="mailto:info@example.com">info@example.com</a>`（mono, text-2）を追加。 | 1440×900 でフッター全高 ≤ 260px、Contact 補足文からフッター1行目までのスクロール距離 ≤ 200px。390 でも同様。 | storyboard §3.8「auto（約40vh）」, §6.5, brief §5.9 |
| 8 | **M** | Works カード（両サイズ）<br>`1440x900-08.png`, `390x844-06.png`, `-07.png` | サムネイル領域が「グラデ + 24px グリッド + 巨大な数字 01/02/03」。どの制作会社のテンプレートにも入っている placeholder の見た目で、3件の違いが数字と色相しかない。brief §5.6 は「`--sd-surface` に 3D シーンの静止フレーム」、storyboard §3.5 は「CSS グラデ + 案件名タイポ」を指定。さらに `work__thumb--2` の surface → secondary グラデは brief §3.1「primary → 透明のみ許可」に外れる。 | 数字と格子を撤去し、各サムネに **案件のカテゴリ語（CORPORATE / SERVICE / BRAND）を mono 12px、案件名を Zen Kaku 500 で左下に組む**。背景は `--sd-surface` 単色＋ inline SVG で各案件に対応する3D状態の線画（01: 格子、02: 結び目、03: 輪。点と線のみ、画像不使用）。`work__thumb--2` のグラデを廃止。 | 3つのサムネが構造的に異なる（数字だけの違いでない）。390 でもサムネ内の案件名が読める（≥ 14px, ≥ 4.5:1）。`#D4A65A` へのグラデが CSS に残っていない。 | brief §5.6, §3.1（グラデ規則）; storyboard §3.5, §7（画像なし） |
| 9 | **M** | モバイル ヘッダー<br>`390x844-00.png`, `-01.png` | `.header__cta{display:none}` が 1024px 未満で有効。storyboard §6.2「sm では非表示にせずアイコン + 短縮ラベル『相談する』」、§6.5-1「ヘッダー右端（常時）」に反し、モバイルではヒーローと Contact 以外で主アクションに届かない。 | `components.css`: `.header__cta` を sm でも表示（`height:36px; padding:0 .75rem; font-size:.8125rem`）し、`.menu-btn` の左に配置。 | 390 幅でヘッダーに `SD ■` / `相談する` / メニューが1行に収まり、折り返しなし。タップ領域 ≥ 36×44px。 | storyboard §6.2, §6.5 |
| 10 | **M** | モバイル Contact<br>`390x844-09.png` | Company の石板が「06 — CONTACT」ラベルと見出しの真後ろに残っている（slab → ring が Contact local 0–0.5 で進むため、見出しが画面中央に来る時点で石板が約70%）。CTA セクションの見出しに3Dが重なる。 | `sections.ts` Contact: モバイルでは slab→ring を local **0–0.25** に短縮し、`groupY` を `-0.3` → **`+0.9`**（輪が見出しの上に浮く）。デスクトップは現状維持。 | 390×844 で Contact の h2 がビューポート中央にある時、h2 / lead のボックスと3Dピクセルが交差しない。 | storyboard §5.2「3Dは画面上部55%」, §6.7 |
| 11 | **M**（検証未了） | Contact（デスクトップ）<br>`1440x900-11.png` | CTAが画面上端にある時点で輪が視認できない（点は opacity ≥ 0.8 のはず）。「輪が CTA を囲む」という Contact の唯一の3D意図が証拠上確認できていない。 | 変更ではなく証拠の追加: Contact local 0.5（h2 が中央）で1枚、CTA が中央で1枚撮る。輪がボタン背後に見えない場合は `pointSize` 1.8 → 2.2、`accentMix` 0.3 → 0.4。 | 2枚のスクリーンショットで、輪の楕円が CTA ブロックを囲んで視認でき、かつ CTA 文字 `#0B0E14` on `#FF6B4A` ≥ 4.5:1。 | storyboard §3.7 |
| 12 | m | カード／ステップ／Works 説明文、Contact 補足<br>全画面 | `.card__body p` `.step__body p` `.work > p:last-child` が 14px × `--sd-text-2`。これらはキャプションではなく事業説明の本文。storyboard §6.7「`--c-muted` は 16px 以上」。 | 上記3つを `font-size: var(--sd-fs-body)`（16px）に。`.note` と `.company-table th` は 14px のまま可（メタ情報）。 | 1440 / 390 で該当段落の computed font-size が 16px。Service ピン内で4枚が 900px に収まる（現状 807px で CTA まで収まっているので +40px 程度は許容）。 | brief §3.2 型スケール（small はキャプション用）, storyboard §6.7 |
| 13 | m | Hero（両サイズ）<br>`1440x900-00.png`, `390x844-00.png` | 点群が一様に散っていて「星空テンプレート」に見える。storyboard T0 は curl noise で塊（星雲）と外殻ハローを作る指定だが、密度差がほとんど見えない。 | `targets` の cloud: curl 振幅 1.2 → **1.8**、ハロー比率 14% は維持。ティール点（10%）の `aSeed` サイズ係数を +0.2。 | Hero local 0 で、画面を 6×4 のブロックに分けた点密度の最大/最小比 ≥ 3。 | storyboard §2.2 T0, brief §6-6「散らばった点群」 |
| 14 | m | コピー照合（`index.html` vs brief §5） | 全セクション一致。差分は2点のみ: (a) Hero 副CTA「実績を見る」は brief §5.2 に無い（storyboard §6.5 の「Works を見る」の和訳、問題なし）。(b) About のキーワード3語は brief §1.5 Values から引用（§5.3 には無いが、ブランドと整合）。 | 変更不要。フッターにメールを追加する際は brief §5.9 の順序（ワードマーク→社名→©→TOP）を崩さない。 | — | brief §5 |

---

## 3. このプロダクト固有で、残すべきもの

1. **タグライン主導のヒーロー**（`1440x900-00.png`）— `SD ■` はナビ左端だけ、ヒーロー中央はタグライン。ラベル `SD — WEB PRODUCTION STUDIO` を mono で添え、CTA はコーラル1つ＋テキストリンク1つ。brief §2, §3.3, §5.2 の通り。
2. **1つの点群が形を変え続ける物語**（`-02` 球 → `-04` 格子 → `-06` 設計図 → `-07` 結び目 → `-10` 石板）— 同じオブジェクトが最後まで語る、という「一貫して手がける小さなスタジオ」の翻訳。Service でカードに合わせて象限が灯る（`-04`）、Process で1ステップだけが白くなる（`-06`〜`-08`）という DOM ↔ 3D の同期は他社サイトにない。
3. **Service / Process を「番号付きリスト」で語る**（真鍮の `01`〜`05`、ティールの縦バー、罫線のみ）— 3枚カードグリッドに逃げていない。密度と沈黙が brief §1.6「精密・静穏」に一致。
4. **色の規律** — コーラル `#FF6B4A` はヒーローと Contact のボタン2つだけ。ラベルは真鍮、リンクとアクティブ状態はティール、面は `--sd-surface`。純黒・純白なし。会社概要は装飾のない右寄せテーブル。
5. **セクションインジケーター**（右端の正方形ドット＋現在地ラベル）— ワードマークの `■` と同じ節点モチーフで、7段階の物語の位置を示す。

---

## 4. Frontend Developer への修正リスト（影響順）

1. **[C] モバイルの Service / Process 減光を廃止** — `sections.ts` でピンなし時は opacity tween を組まず、個別 reveal で opacity 1。CSS 初期 opacity を `min-width:1024px` に限定。検証: `390x844-03` 相当で4枚とも白。
2. **[C] Works → Company の真鍮メッシュと空白** — `knotOpacity` 上限 0.55、フェード local 0.6–0.8、Works `camZ` 6.2、Works 120vh / Company 100vh 上寄せ。検証: Works local 0.5 でカード3枚＋真鍮面積 ≤ 10%、テキストなしの連続スクロール ≤ 50vh。
3. **[M] デスクトップの非アクティブ opacity 0.3/0.35 → 0.6、最初の項目をピン開始直後（local 0.05）に点灯**。検証: ピン開始フレームに白い項目が1つ以上。
4. **[M] About のスクリムを `scrim-r` に、`X_ABOUT` を +0.3** — 球を見出し下の空き領域へ。検証: 本文カラム直下に点群なし、≥ 4.5:1。
5. **[M] Service / Process の3Dをテキスト列の外へ** — `X_SERVICE -1.9`, `X_PROCESS -1.5`（または本文 `max-width:30em`）。検証: local 0.5 で x < 720px にテキストと交差する3Dなし。
6. **[M] モバイルの `min-height` を auto に** — About / Works / Company / Contact、`padding: 4rem 0`。Contact はモバイルで slab→ring を local 0–0.25、`groupY +0.9`。検証: 200px 刻みでテキストなしフレームなし、h2 に3D非交差。
7. **[M] フッターの空洞** — `min-height:0`、gap 縮小、メールアドレス追加、Contact `padding-bottom` 4rem。検証: フッター全高 ≤ 260px。
8. **[M] Works サムネの汎用プレースホルダを置換** — 数字と格子を外し、カテゴリ＋案件名のタイポ＋ inline SVG の線画（格子／結び目／輪）。`work__thumb--2` の真鍮グラデ廃止。検証: 3枚が構造的に異なり、390 で案件名可読。
9. **[M] モバイルヘッダーに `相談する` を常時表示**（36px、メニュー左）。検証: 390 幅で1行。
10. **[M/検証] Contact の輪** — local 0.5 と CTA 中央の2枚を撮影し添付。見えなければ `pointSize 2.2 / accentMix 0.4`。  
    （Minor は余力があれば: 説明文 16px 化、Hero 点群の塊の強調。）

---

## 5. PASS 条件

- 390×844 で Service カード4枚・Process ステップ5つが、画面内にある間は常に opacity 1 で読める（合成 ≥ 4.5:1）。
- 1440×900 で、Hero 〜 Footer のどのスクロール位置でも「テキストが一切ないビューポート」が 50vh を超えて続かない（Works→Company の1箇所を含む）。
- Works local 0.5 で真鍮色の面積が画面の 10% 以下、かつカード3枚が見える。
- About / Service / Process の local 0.5 で、本文・カード・ステップの行ボックスに3Dのピクセルが交差せず、DevTools 合成スクリーンショットで ≥ 4.5:1。
- モバイルのヘッダーに `相談する` が常時表示される。
- Works サムネに数字だけのプレースホルダが残っていない。
- フッター全高 ≤ 260px、メールアドレスあり。
- Contact の輪が CTA を囲む証拠スクリーンショットが2枚添付されている。
- 上記をすべて満たした状態で、Hero のタグライン・ワードマーク・色の規律・番号付きリスト・インジケーターが変更されていないこと（§3 の「残すべきもの」）。

---

## 対応記録（Frontend Developer, 2026-09-23）

| # | 対応 |
|---|---|
| 1 | ピンなし（モバイル／タブレット）ではカード・ステップの opacity を scrub せず、要素ごとの入場（`start: 'top 85%'`）で常時 opacity 1。縦バーの点灯のみセクション進行に連動。CSS 初期値は `min-width: 1024px` に限定 |
| 2 | `knotOpacity` 上限 0.55、フェードアウトを local 0.6–0.8 に前倒し、Works カメラ `camZ` 6.2。Works 120vh / Company 110vh に短縮 |
| 3 | 非アクティブ opacity 0.6。Service カード1は local 0.12、Process ステップ01は 0.05 で点灯 |
| 4 | About は見出し・本文とも左 6 列に揃え、球は右（`X_ABOUT -1.1`）。スクリムは左のまま |
| 5 | `X_SERVICE -1.9`, `X_PROCESS -1.5`。説明文に `max-width: 30em` |
| 6 | `max-width: 767px` で About / Works / Company / Contact の `min-height: auto`、余白を `--space-16` に |
| 7 | フッター `min-height` 撤廃、gap 縮小、`info@example.com` の mailto を追加 |
| 8 | サムネイルを `--sd-surface` + inline SVG（01 格子 / 02 結び目 / 03 輪）+ mono ラベルに置換。secondary へのグラデを削除 |
| 9 | ヘッダー CTA「相談する」をモバイルでも表示（36px 高） |
| 10 | Contact のトリガーを `top 45%` 開始にし、モバイルは slab→ring を local 0–0.3、`groupY +0.9` |
| 11 | 1440×900 で h2 中央・CTA 中央の 2 枚を撮影し、輪が CTA ブロックを囲むことを確認 |
| 12 | カード／ステップ／Works 説明文を 16px に |
| 13 | Hero の cloud ターゲットのノイズ振幅 1.2 → 1.8 |
| 14 | 変更なし |

---

## 再審査（第2回）

**レビュー**: UI Finish-Gate Reviewer（2026-09-23）
**対象ビルド**: `9eb7b57 Apply UI finish-gate review findings` + `7ff8d40`（`index.html` / `src/styles/*.css` / `src/scroll/sections.ts`）
**証拠**: `scratchpad/shots/1440x900-00..12.png` と `-about-body / -works-cards / -company-table / -contact-h2 / -contact-cta.png`（デスクトップ）, `scratchpad/shots-m/390x844-00..10.png` と同名の targeted 5枚（モバイル）
**注記**: 第1回と同じくサンドボックスで Google Fonts が読めていないため、書体は判定対象外。
**測定**: 3Dがテキスト列に残る箇所は目視に加え、スクリーンショットの行間（テキストを含まない帯）の相対輝度を直接測った（純Python PNGデコード、WCAG 相対輝度）。storyboard §6.7 の上限 `#2A2F3A` は L=0.028、`--sd-text-2` が 4.5:1 を保てる背景は L≤0.042。

### 1. 判定

### PASS

理由: 第1回の Critical 2件は両方とも観測可能に解消した。(1) モバイルの Service カード4枚・Process ステップ5つは画面内で常に opacity 1（`390x844-03.png`, `-05.png`）で、事業内容と工程が読める。(2) Works→Company は、結び目の実体が 0.55 で「カード3枚の背後」に収まり（`1440x900-works-cards.png`, `-10.png`）、真鍮色は Fresnel のリム線と点だけになった。Works 120vh／Company 110vh 化で、デスクトップのカード下端から会社概要ラベルまでの空きは約545px（< 900px ビューポート）となり、テキストのないビューポートはどのスクロール位置にも存在しない。Major 9件も全て解消、残るのは「3Dの薄い縁がテキスト列の端に掛かる」種類の残差 2件（#4 About, #5 Process 0.32–0.49）で、いずれも行間の実測背景輝度が p90 ≤ 0.007（上限 0.028 の 1/4）、合成コントラスト ≥ 7.3:1 と、§6.7 の受け入れ基準を満たしている。修正で新たに持ち込まれた Critical／Major はない。第1回 §3 で「残すべき」とした 5 点（タグライン主導のヒーロー、1つの点群が7つの形に変わる物語、番号付きリスト、色の規律、インジケーター）は全て手つかずで残っている。

### 2. 第1回所見（14件）の再確認

凡例: **解消** = 所見に書いた製品上の問題が観測できない／**部分的** = 減ったが観測できる（阻害しない場合はその旨を明記）／**未解消**

| # | 元の重大度 | 判定 | 証拠 | 確認内容 |
|---|---|---|---|---|
| 1 | C | **解消** | `390x844-03.png`, `-04.png`, `-05.png` | カード4枚・ステップ5つが全て白（opacity 1）。`sections.ts` `revealEach()` がピンなし時に `opacity:1` を `gsap.set` し、`--bar` だけをセクション進行に残している。CSS の `.card{opacity:.6}` `.step{opacity:.6}` は `@media (min-width:1024px)` 限定。`-03` の見出し `#F2F4F7`、本文 `#9AA3B2` はそのまま 17.5:1 / 7.6:1。 |
| 2 | C | **解消** | `1440x900-works-cards.png`, `-10.png`, `390x844-07.png`, `-08.png` | `knotOpacity` 上限 0.55、フェード local 0.6–0.8、`camZ` 6.2。`-works-cards` でカード3枚が全て見え、実体は薄いリム線＋点で、面としての真鍮色は残らない。Works 120vh／Company 110vh: カード下端→Company ラベルの空き ≈ 247 + 297 = 約545px < 900px なので、テキストのないビューポートは生じない。モバイルは `-08` でカード3→ラベル 140px。 |
| 3 | M | **解消** | `1440x900-03.png`, `-06.png` | ピン開始相当のフレームで Service カード01、Process ステップ01 が白。非アクティブ 0.6 の見出し（`-03` のカード02–04、`-06` のステップ02–05）は灰色だが判読でき、`#F2F4F7`@0.6 ≈ 6.7:1。 |
| 4 | M | **解消**（別解） | `1440x900-about-body.png`, `-01.png` | 指示（scrim-r・球を左）ではなく、見出しと本文を左6列に揃えて球を右（`X_ABOUT -1.1`）に置く別解。本文が球の真上に載る状態は消えた。残差: 球の縁（x≈600–705）が本文各行の末尾 ~80px に掛かる。実測: 段落間の帯 x600–705 の背景 L p90 = 0.007（上限 0.028）、合成 16:1。読みに影響なし。 |
| 5 | M | **部分的**（非ブロッキング） | `1440x900-03.png`, `-04.png`（Service）, `-07.png`, `-08.png`（Process） | Service: 格子の左端 x≈700–720、カード本文右端 ≤690（`max-width:30em`）。カード02/03 間の帯 x600–700 の背景 L = 0.006、交差なし。Process: `-08`（local ≥0.66）は結び目が x≥630 で交差なし。ただし `-07`（local 0.32–0.49、grid→knot の変形中 `noise 0.18`）では拡散した点が x≈430 までステップ02–04 の行に掛かる。実測: ステップ間の帯 x430–607 の背景 L p90 = 0.006、`--sd-text-2` との合成 7.3:1 で §6.7 は満たす。「線が食い込む」元の Major は解消、残差は Minor に降格。任意で `seg(tl, 0.32, 0.49, …)` の `noise` 到達値 0.18→0.10、または同区間だけ `tX` を −1.5→−2.0。 |
| 6 | M | **解消** | `390x844-02.png`, `-04.png`, `-06.png`, `-08.png`, `-09.png` | モバイルのセクション間の空き: About→Service 150px, Service→Process ≈150px, Process→Works 170px, Works→Company 140px, Company→Contact 170px。いずれも 200px 刻みでテキストなしフレームが出ない。デスクトップも Hero→About ≈620, About→Service ≈500, Process→Works ≈280, Company→Contact ≈530（全て < 900）。 |
| 7 | M | **解消** | `1440x900-11.png`, `-12.png`, `390x844-10.png` | フッター全高 165px（罫線 y735 → 下端 900）、`info@example.com` の mailto あり、brief §5.9 の順序（ワードマーク→社名→©→TOP）は保持しメールは社名の直後。補足文→フッター1行目は 321px で目標 200px を超えるが、その区間には輪が置かれ「空洞」ではない。モバイルのフッターは約300px（ナビが2行に折れるため）だが内容で埋まっている。 |
| 8 | M | **解消** | `1440x900-works-cards.png`, `390x844-07.png`, `-08.png`, `-works-cards.png` | 数字と24pxグリッドを撤去。各サムネは `--sd-surface` ＋ inline SVG（01 格子／02 結び目／03 輪）＋ mono タグ `SAMPLE 0n — 形名`、下にカテゴリ（真鍮 mono）と案件名（500）。3枚が構造的に異なる。`work__thumb--2` のグラデは削除、残る `--1` `--3` は primary→透明のみ（brief §3.1 準拠）。モバイルの案件名 17px 白で可読。 |
| 9 | M | **解消** | `390x844-00.png`, `-01.png`, `-about-body.png` | ヘッダーに `SD ■` / `相談する`（outline 36px）/ メニューが1行。折り返しなし。 |
| 10 | M | **解消** | `390x844-contact-h2.png`, `-09.png` | 石板は Contact 到達前に消え（`-09` で痕跡の点だけ）、h2「まずは、お話から。」とリードのボックスに3Dは交差しない。輪は見出しの上（y≈275–370）に浮き、ラベル直上の帯の背景 L = 0.012。 |
| 11 | M（検証未了） | **解消** | `1440x900-contact-h2.png`, `-contact-cta.png` | h2 中央のフレームで輪が h2＋リードを囲み、CTA 中央のフレームでは輪がボタンを囲む楕円として視認できる。CTA 文字 `#0B0E14` on `#FF6B4A` 6.9:1。 |
| 12 | m | **解消** | `components.css`, `1440x900-04.png` | `.card__body p` `.step__body p` `.work > p:last-child` が `var(--sd-fs-body)`（16px）。Service ピン内は4枚＋CTA が y≈833 で 900px に収まる。 |
| 13 | m | **部分的**（Minor のまま） | `1440x900-00.png`, `390x844-00.png` | 上部中央にやや濃い帯はできたが、星雲の塊と外殻ハローの差はまだ弱く、6×4 ブロックの密度比 ≥3 は証拠上確認できない。阻害しない。 |
| 14 | m | **変更なし** | `index.html` | コピーは brief §5 と一致。フッター順序は維持。 |

### 3. 修正で新たに生じた所見（Critical／Major）

**なし。**

Critical／Major に該当しないが、次回の余力で見ておく観察（3件、いずれも実測で §6.7 を満たしており PASS を阻まない）:

- **モバイル Works の見出しブロック**（`390x844-06.png`）— Works にはスクリムがなく（カードが背景を持つ前提）、モバイルでは高さを auto にしたぶん結び目が「実績」見出し・リード・注記の真後ろに来る。実測: リード／注記間の帯の背景 L 0.015（p90 0.012）、注記 `--sd-text-2` との合成 6.3:1。ページで最も余裕が小さい点。任意で `@media (max-width:767px){ .section--works::before{ background: 縦グラデ（scrim-l と同じもの） } }`。注記自体は公開時に消える文言。
- **モバイル Contact の CTA 中央フレーム**（`390x844-contact-cta.png`）— 輪がほぼ真横から見え「帯」としてリード1–3行目の背後を横切る。行間の背景 L 0.006、合成 17:1 で可読性は問題ないが、「輪が囲む」意図はモバイルでは読み取れない。任意で モバイル時のみ `rotX` を足して楕円に見せる。
- **#5 の Process 0.32–0.49、#13 の Hero 密度差** — 上表のとおり。

### 4. 第1回 PASS 条件との照合

- 390×844 で Service 4枚・Process 5つが常に opacity 1 — **満たす**（`-03`, `-05`）
- 1440×900 でテキストのないビューポートが 50vh を超えて続かない — **満たす**（最大の空きは Works→Company 約545px、ビューポート内に必ずテキストが残る）
- Works local 0.5 で真鍮面積 ≤10% かつカード3枚 — **満たす**（`-works-cards`: 実体はリム線のみ）
- About／Service／Process の local 0.5 で本文行と3Dが交差せず ≥4.5:1 — **合成コントラストは満たす**（最小 7.3:1）。交差については About の縁 ~80px、Process 0.32–0.49 の拡散点が残るが上記のとおり Minor
- モバイルヘッダーに `相談する` 常時表示 — **満たす**（`390x844-00`）
- Works サムネに数字だけのプレースホルダなし — **満たす**
- フッター ≤260px、メールあり — **満たす**（165px）
- Contact の輪の証拠2枚 — **満たす**（`-contact-h2`, `-contact-cta`）
- 「残すべきもの」5点が無傷 — **満たす**

出荷可。上記 §3 の観察は次のイテレーションの任意項目とし、この判定の条件にはしない。

---

## 再審査（第3回・品質パス）

**レビュー**: UI Finish-Gate Reviewer（2026-09-23）
**対象ビルド**: `bd5a357`（宇宙配色）→ `e682d12`（渦巻銀河）→ `85f0d82`（惑星）→ `a0ad45c`（ポスト処理・プリローダー・カーソル・マイクロインタラクション）
**契約**: `docs/brand-brief.md`（§3.1 v1.1 配色注記を含む）, `docs/scroll-storyboard.md`
**証拠**: `scratchpad/shots/1440x900-00.png`（Hero）, `-01.png`（Contact→Footer）, `-about-body / -works-cards / -company-table / -contact-h2 / -contact-cta.png`, `loader-early.png`; `scratchpad/shots-m/390x844-00..04.png` と同名 targeted 5枚
**注記**:
- 書体はサンドボックスのフォールバックのため判定対象外。ヘッドレス（swiftshader）は実 GPU より bloom がやや強く出るため、発光面積は「上限側の見積もり」として読む。
- 依頼にあった `hero-after.png` / `hero-cursor.png` は提出物に存在しない。`loader-early.png` はプリローダーが退場（`yPercent`）を始めた後のフレーム（下端 36px に本体が見えている）で、ワードマーク＋カウンターの状態は写っていない。よって **カーソル・磁力ボタン・Works のチルト・プリローダーの計数表示は、スクリーンショットではなくコードで判定**した。
- 測定は第2回と同じ純 Python の PNG デコード + WCAG 相対輝度。「発光面積」は L > 0.12 の画素率（DOM の白文字・CTA を含むので実際の 3D 発光はこれより小さい）。「テキスト直下」は行間の帯の背景輝度 p90 と、その帯に対する文字色の合成コントラスト。storyboard §6.7 の上限 `#2A2F3A` は L = 0.028。
- ビルド実測: JS gzip 合計 197 kB（three 134 / gsap 44 / app 19）、CSS 5.2 kB。storyboard §7 の 230 kB 以内（bloom 込み）。

### 1. 判定

### HOLD

理由: 「宇宙」への転換そのものは成功している。渦巻銀河の腕がタグラインの背後を斜めに抜け、About の核が惑星になり、Contact の輪に光が周回する、という物語は v1.0 の骨格（点 → 核 → 格子 → 設計図 → 結び目 → 礎 → 輪）を一切崩していない。発光面積は全フレームで brief §3.1 の 5% を下回り（DOM 込みで 1.3〜4.2%、モバイルの超過分は見出しと CTA ボタン）、bloom（strength 0.32 / threshold 0.72）は「点と線の縁を柔らかくする」範囲に収まっている。ここまでは制作会社の自社サイトとして通用する。

出荷を止めるのは次の3点である。(1) **Contact — サイトで唯一の変換点 — で、ページ最大の発光体である輪と周回光がリード文の文字の上を通る。** デスクトップの h2 中央フレームで、リード1行目の先頭3文字・末尾3文字の背後に輪の帯があり行間の背景 p90 = 0.197、白文字との合成 **3.9:1**（`1440x900-contact-h2.png`）。モバイルでは真横から見た輪が帯になってリード1〜3行目を横切り、周回光（白球 16px）が「まだ要件が…」の「まだ」に重なって **1.2:1**（`390x844-contact-cta.png`）。光は時間駆動で約14秒に1周するため、モバイルの全訪問者が読んでいる最中に文字が消える瞬間を必ず見る。storyboard §6.7 の受け入れ基準（local 0.5 で 4.5:1）の明確な違反で、第2回で PASS した箇所が bloom と周回光で後退した。(2) **インタラクション層が自分自身と衝突している。** `.btn` と `.work` の CSS に `transition: transform` が残ったまま、GSAP `quickTo` が毎フレーム inline transform を書く（磁力ボタン・チルト）。毎フレーム 280ms のトランジションが起動し直すので追従はもたつき、inline transform が `:hover { transform }` を上書きするため、CTA の `scale(1.02)` と Works カードの `translateY(-4px)`（第2回で「残すべき」とした挙動）は実機では発火しない。「Web制作会社クオリティ」は、こういう細部が破綻していないことで判定される。(3) **プリローダーが「読み込むもののないサイト」で毎回 2 秒近く画面を塞ぐ。** 画像なし・フォントは `display=swap` なのに最短 1.1s の足止め + 退場 1.7s。ヒーロー見出しの表示開始は約 1.9〜2.0s、文字出しの完了は約 3.2s で、storyboard §7 の LCP ≤ 2.0s を自ら破る。再訪でもスキップしない。

加えて、実装が契約文書から静かに離れた箇所が3つある（ポスト処理は storyboard §2.3「ポストプロセスなし」§8-15「`postprocessing` の import なし」で明示的に禁止、ヒーローの環付き惑星は brief §3.5-4「現実の物体を模さない」に抵触、`--sd-fs-display` 等のトークン変更は brief §3.1 v1.1 注記「配色のみ変更」と矛盾）。いずれもクライアント要望の正当な結果と読めるが、**契約側を v1.2 に更新するか実装を戻すかを決めないまま出荷しない**。

### 2. スタジオ級として読める箇所（残すもの）

1. **Hero の銀河とタグラインの関係**（`1440x900-00.png`）— 腕が左下から右上へ抜け、核（最も明るい塊、L p90 0.55）が「動き出す。」の句点の直後 (720, 430) に置かれている。見出し・リード・CTA は腕の暗い側にあり、リード行間の背景 p90 = 0.011（15.6:1）、「実績を見る →」の下も 0.013。銀河は装飾ではなく「構造が動き出す」の絵になっている。
2. **About の核 = 惑星**（`1440x900-about-body.png`）— 本体は `#152650`（L 0.017）で暗く、光るのは水色のリムと点の輪だけ。brief §6-2「素材は非発光の面 + 発光する縁・粒子」の宇宙版になっている。本文は左6列に収まり、球は見出し下の空きに入る。
3. **発光の規律**（全フレーム）— L > 0.12 の面積: Hero 4.2%（106px 見出し込み）/ About 3.3% / Works 1.6% / Company 1.3% / Contact 2.2%。Works の金色は 0.4%（第1回の PASS 条件 ≤10% を大幅に下回る）。bloom 後でも「暗さが主役」が守られている。
4. **Works の実体**（`1440x900-works-cards.png`）— 結び目は 0.55 のまま bloom で縁だけが持ち上がり、カード内の行間は p90 ≤ 0.016（≥7:1）。サムネの inline SVG 線画は宇宙配色でもそのまま成立している。
5. **Company の静けさ**（`1440x900-company-table.png`）— 石板は白点の粒だけ、行間 p90 0.033（11.5:1）、表は装飾なし。ページで最も動かないフレームが最も信頼を語る。
6. **セクション番号とフッターの巨大ワードマーク** — `stroke 0.07` / `color 0.035` と極端に薄く、既存の `01 — ABOUT` モノラベル体系の拡張として読める。Contact の「06」が右上の星雲に重なる構図（`-contact-h2.png`）は良い。
7. **モバイルの基礎**（`390x844-01..03`, `-company-table`）— Service カード・Process ステップは常時可読、ヘッダーに `相談する`、セクション間の空白なし。第2回の到達点は保たれている。
8. **バンドル** — 197 kB gzip、`three/addons` を入れても予算内。

### 3. 所見（重大度順）

凡例: **C** = Critical（出荷停止）, **M** = Major（PASS 前に必須）, **m** = Minor（推奨。PASS は阻まない）

| # | 重大度 | 画面 / 証拠 | 何が起きているか | 観測可能な変更 | 検証条件 |
|---|---|---|---|---|---|
| 1 | **C** | Contact（両サイズ）<br>`1440x900-contact-h2.png`, `390x844-contact-cta.png`, `390x844-04.png` | **デスクトップ**: h2 中央フレームで輪の帯（幅 40〜60px）がリード1行目の左端 x 368–450 と右端 x 1000–1075 の背後を通る。行間左端 p90 = 0.197（3.9:1）、右端 0.154（4.7:1 ぎりぎり）、2行目右横の帯 0.276。第2回（bloom なし・pointSize 2.0）では囲んで見えていた輪が、bloom + `accentMix 0.4` で「文字を横切る光の帯」になった。周回光（`orbitMesh` 0.07 + glow 0.16, 不透明度 0.8）は帯に沿って動くので、1行目の先頭・末尾を約14秒ごとに通過する。<br>**モバイル**: 輪はほぼ真横（`rotX 0`）で、`groupY +0.9` のまま canvas に固定されるためスクロール中はビューポート y 150–300 の帯としてラベル・h2・リード1〜3行目を順に横切る。周回光はモバイルの方が明るい設定（`orbitMat 1.0 / glow 0.35`）で、`-contact-cta` では (12, 200) の白球が「まだ」に重なり p90 0.73 → **1.2:1**。行間1 p90 0.024、行間2 max 0.64。 | **デスクトップ** (a) `sections.ts` Contact: 輪がリードのブロックを **外側から** 囲むよう `camZ 7.5 → 6.9`（輪の帯が x ≤ 345 / ≥ 1095 に出る。リードは x 368–1075）。(b) `sections.css` `.scrim-c::before` を `radial-gradient(72% 58% at 50% 50%, rgba(8,11,28,.85) 0%, rgba(8,11,28,.6) 50%, transparent 100%)` に強める。(c) `scene.ts` 周回光: `orbitMat.opacity` 0.8 → 0.6、glow 半径 0.16 → 0.12。<br>**モバイル** (d) Contact の `rotX` を `0 → 0.55`（楕円として見せる。第2回 §3 の任意項目を必須化）、`groupY +0.9 → −0.25`（楕円の中心を CTA ボタンに合わせ、帯は h2 の上と補足文の下を通る）。(e) 周回光はモバイルで `orbitMat 0.5 / glow 0.12` に下げるか、`orbitOpacity` の到達値を 0 にする。(f) 帯が文字列を通らざるを得ない位置では `.section--contact` にモバイル用の中央スクリム（`scrim-c` と同式、alpha .8）を敷く。 | 1440×900 で h2 中央フレームと CTA 中央フレームの2枚: リード各行の左右 100px の行間帯で背景 p90 ≤ 0.028、合成 ≥ 4.5:1。周回光を 0 / 90 / 180 / 270° の4位相で撮り、白球の L > 0.2 画素がリード・補足文・メールの行ボックスと交差しない。390×844 でラベル〜補足文まで 100px 刻みで撮り、輪と光の L > 0.05 画素がどのテキスト行ボックスとも交差しない。 |
| 2 | **M** | カーソル・磁力ボタン・Works チルト（デスクトップ）<br>`components.css` L43–59, 227–236, 310–320, 336–343; `cursor.ts` L37–48; `effects.ts` L41–56（スクリーンショット未提出） | (a) `.btn { transition: transform .28s }` と `.work { transition: transform .28s }` の上で GSAP `quickTo('x'/'y'/'rotationX'/'rotationY')` が毎フレーム inline transform を書く。フレームごとに CSS トランジションが再起動するため磁力とチルトは設定値（0.45s / 0.5s）どおりに追従せず、離した後の戻りも二重に補間される。(b) inline transform は `:hover { transform }` より優先されるので、`.btn--signal:hover { scale(1.02) }` と `.work:hover { translateY(-4px) }` は磁力・チルトが一度でも動いた要素では二度と発火しない（GSAP は 0 に戻しても `translate(0, 0)` を inline に残す）。(c) `is-text` クラスに CSS がなく、`data-cursor="text"` を持つ要素もない。`cursor: none` が html 全体に掛かるので本文上で I ビームが消え、テキスト選択の手がかりがない。(d) カーソルは「丸い点 + 遅れて追う丸い輪」— brief §3.3 が「唯一のシンボル要素」と定めた `■` と無関係な二つ目の記号を、全ページの最前面（z 70）に常時置いている。この組合せは制作会社サイトで最も交換可能なパターンで、SD のものである理由がない。 | (a) `.btn` / `.work` の `transition` から `transform` を外す（`background-color, color, border-color, box-shadow` のみ残す）。ホバーの持ち上がりは CSS ではなく同じ `quickTo` 系で `y: -4` / `scale: 1.02` を与える（GSAP に一元化）。(b) `.cursor__dot` を 6×6 の正方形（`border-radius: 0`, `--sd-primary`）にし、輪は撤去。ホバー時は同じ正方形を `scale(4)` + `background: transparent; outline: 1px solid var(--sd-primary)` に変化させる（ワードマークの `■` と同一モチーフ）。`p, li, td, h1–h3` 上では `is-text` で正方形を 2×16 の縦線（I ビーム相当）にする CSS を追加。(c) `.work` は #3 の対応後にチルト対象から外す。 | DevTools Performance で CTA 上をポインタが横切る 2 秒間を記録し、`.btn` の transform が transition イベントを発生させない（`transitionrun` 0 回）。CTA ホバー中に computed `transform` に `scale(1.02)` 相当（matrix a=d≈1.02）が含まれる。本文段落上でカーソルが縦線になるスクリーンショット 1 枚、CTA 上で `■` が拡大したスクリーンショット 1 枚（`hero-cursor.png` の再提出）。 |
| 3 | **M** | Works カード（デスクトップ）<br>`effects.ts` `setupTilt`, `components.css` L336–343, `index.html` `<li class="work">` | カードは `<li>` で、リンクも詳細ページもない（storyboard §3.5「Works 詳細ページはなし」）。そこに ±5°/±6° のチルト + 追従する水色のシーン + ホバー持ち上げ（#2 で死んでいる）を与えると、「押せる」というアフォーダンスだけが立ち上がり、押しても何も起きない。カーソルの `is-hover` は `a, button` にしか反応しないので、同じ要素で「カードは反応する／カーソルは反応しない」という矛盾した信号が出る。 | `setupTilt()` の対象を `a.work, .work[href]` に限定し、現状の `<li>` には適用しない（実案件に差し替えて `<a>` になった時点で自動的に有効化される）。`.work::after` のシーンも同じ条件に。ホバーは第2回どおり枠線色の変化（`--sd-secondary`）のみ残す。 | 1440×900 で Works カード上にポインタを置き、computed `transform` が `none`（または reveal 後の `translate(0, 0)`）のまま、`border-color` だけが変わるスクリーンショット。 |
| 4 | **M** | プリローダー<br>`effects.ts` `runPreloader`, `main.ts` L96–100, `components.css` L297–307; `loader-early.png`（退場中フレームのみ） | 読み込む資産がない（画像なし、フォントは `swap`、3D は手続き生成）のに `minTime 1100ms` で足止めし、退場に 0.35 + 0.1 + 0.35 + 0.9 = 1.7s。`heroIntro()` は退場開始時に呼ばれるので見出しの文字出しは約 1.9〜2.0s から始まり、`stagger 0.04 × 9 + 1.1s` で約 3.2s に完了。storyboard §7「LCP ≤ 2.0s、LCP 要素は Hero 見出し」を演出で破っている。`sessionStorage` 等の再訪判定がなく、ナビ→戻るで戻って来るたびに同じ 2.8s を見る。表示は「ワードマーク + 160px の線 + 000→100 の3桁カウンター」で、カウンターは制作会社サイトの既定値そのもの（線はブランドの `.rule` と同じ言語なので残せる）。 | (a) `minTime` 1100 → **450ms**、退場を `inner フェード 0.25s` + `yPercent −100 0.6s` に短縮（合計 ≤ 1.3s、見出し開始 ≤ 1.0s）。(b) `sessionStorage.getItem('sd-seen')` があれば `reduced` 経路（0.3s フェード）を使う。(c) カウンターの数字は撤去し、線の `scaleX` だけを進行に使う。線の右端に 6×6 の `■` を置き、100% で `--sd-primary` に点灯 → その `■` が退場後にヘッダーのワードマークの `■` の位置へ移動する（プリローダーとブランド記号を結ぶ）。時間がなければ (a)(b) だけで可。 | Lighthouse（モバイル/デスクトップ）で LCP ≤ 2.0s、LCP 要素が `.hero__title`。初回訪問の動画で、見出し1文字目の表示が t ≤ 1.0s。同一セッション2回目のロードでプリローダーが 0.4s 以内に消える。 |
| 5 | **M** | ポスト処理（デスクトップ）<br>`post.ts`, `scene.ts` L64–68; storyboard §2.3, §7, §8-15 | storyboard は §2.3「「Bloom 風」: ポストプロセスなし（グロー層で代替）」と §8-15「`three/examples/jsm/postprocessing` の import なし」で **明示的に禁止**している。禁止の理由は §7 の 3D フレーム時間予算（≤ 6ms 高性能 / ≤ 10ms ミドル）で、`UnrealBloomPass` は DPR 2 の 1440×900 で 2880×1800 のフル解像度から 5 段のミップをブラーする最も重いパスの一つ。今回の提出に frame-time の証拠がない（dev オーバーレイの値も、`renderer.info` も添付なし）。視覚結果は §3.1 の 5% 内に収まっているので、問題は「見た目」ではなく「契約と予算の未確認」。 | (a) 実 GPU（M1 / RTX 級 と ミドル Windows ノート）で Hero / Works / Contact 各 10 秒の平均 frame time を dev オーバーレイで測り、値をこの文書に追記する。(b) 予算超過なら `UnrealBloomPass` の resolution を `(w/2, h/2)`、post 有効時の `maxDpr` を 1.5 に。(c) storyboard §2.3 / §8-15 を v1.2 として「デスクトップのみ bloom 可、予算 ≤ 6ms 維持、モバイルは直描画」と書き換える（Storyteller / UX Architect の承認記録を残す）。 | frame time の実測値が §7 の予算内であること（数値を添付）。`prefers-reduced-motion` で `renderOnce()` のみ、grain が静止していること。 |
| 6 | **M** | Hero の環付き惑星（両サイズ）<br>`1440x900-00.png` (1145, 170), `390x844-00.png` (320, 130); `scene.ts` L181–194 | brief §3.5-4「3Dシーンで製品や人物・現実の物体を模さない（抽象的な幾何・粒子・線のみ）」。v1.1 注記は「配色のみ変更、構造・モーション・コピーは v1.0 のまま」なので、この禁止は生きている。About の惑星は点の輪 + 暗い球で「核」の役を持ち抽象性を保っているが、Hero 右上の小惑星は **不透明な球 + 平らな環** の土星アイコンで、ページの物語（点 → 核 → …）のどこにも登場せず、SD でなくても置ける唯一の要素。紫の縁が bloom で滲み、モバイルでは `相談する` ボタンの真下に来る。 | どちらか: (a) `farGroup` を撤去し、その位置には銀河の外殻ハローの点を少し濃くする（`aGal` 上位の点を右上へ偏らせる）。(b) 残すなら同じ点システムで作り直す — 半径 0.4 の Fibonacci 球（400 点）+ 点で描いた環（200 点）、面は使わない。その上で brief §3.5-4 を v1.2「惑星は点と縁のみで表現し、面で塗った天体は置かない」に更新し、クライアントの「惑星」要望をブリーフに記録する。 | 1440×900 / 390×844 の Hero で、面（連続した塗り）として描かれた天体が About の核以外に存在しない。brief に v1.2 の追記がある。 |
| 7 | **M** | モバイル Hero ラベル<br>`390x844-00.png` | 銀河の核（L p90 0.11、最大 0.90 の点密集）が (120–260, 150–300) にあり、`SD — WEB PRODUCTION STUDIO`（`--sd-text-2` 12px mono, y 242）がその真上に載る。ラベル上下の帯の p90 は 0.0275 / 0.0142 で §6.7 の上限 0.028 に張り付き、max 0.77 の星が 12px の字画に直接掛かる。storyboard §5.2「3D は画面上部 55%」は守られているが、その 55% の中で最も明るい塊を最も小さい文字の背後に置いている。 | `sections.ts` Hero: モバイルの `heroY` 1.2 → **1.7**（核がラベルの上、y ≈ 150 に上がる）。または `.hero__label` をモバイルで見出しの下（`hero__en` の位置）へ移動。 | 390×844 Hero で、ラベルの行ボックス ±6px の帯の背景 p90 ≤ 0.012、L > 0.2 の点がラベルの行ボックスと交差しない。 |
| 8 | m | フィルムグレイン（デスクトップ）<br>`post.ts` `uGrain 0.04` | `(n − 0.5) × 0.04` = 線形 ±0.02 を毎フレーム別のハッシュで加算。背景 `#080b1c`（L 0.0036）に対して 8bit で ±5 階調のノイズが **画面全体で常時明滅** する。brief §3.4-3「動きの後に必ず静止時間を置く」§1.6「静穏」に反し、静止時にも画面が落ち着かない。暗部で最も目立つ。 | `uGrain` 0.04 → **0.018**、`uTime` の更新を 12fps に間引く（`floor(time * 12) / 12`）か、静止ハッシュにする。 | 1440×900 の Company フレームを 1 秒間隔で 3 枚撮り、背景 L < 0.01 の領域で画素差分の p95 ≤ 2 階調。 |
| 9 | m | 二重ビネット<br>`background.ts` L73–74, `post.ts` `uVignette 0.26`, `base.css` `.bg::after` | 背景シェーダ（隅 ×0.6）と grade（隅 ×0.74）でビネットが二重に掛かり、四隅は L 0.0017〜0.0037（背景トークンの 50%、≈ `#04061a`）。純黒ではないが brief Don't-1 の境界にあり、grain と合わさって帯状の階調割れが出やすい。CSS の `.bg::after` は不透明 canvas の下で無効なので放置可。 | grade の `uVignette` を 0 にし、背景シェーダ側だけに統一（または逆）。 | 四隅 40×40 の平均 L ≥ 0.0025（背景トークンの 70% 以上）。 |
| 10 | m | About 本文と惑星のリム（デスクトップ）<br>`1440x900-about-body.png` | 段落間の帯 x 560–705 の p90 = 0.041 / 0.032。合成 10.4:1 で読めるが、§6.7 の上限 0.028 を超えた（第2回は 0.007）。リムの bloom が本文の行末 60px に掛かっている。 | `X_ABOUT` −1.1 → **−1.3**、または `planetMesh` の半径 1.9 → 1.75。 | 同帯の p90 ≤ 0.028。 |
| 11 | m | モバイルの層の重なり<br>`390x844-01.png`, `-about-body.png` | About の惑星（`planetOpacity` の減光が local 0.8–1.0）はモバイルでは About が auto 高さのため Service の見出し・リードの真後ろまで残る（行間 p90 0.019–0.038、可読）。そこに 7rem の「02」も重なり、惑星 + 番号 + 見出しの三層になる。 | モバイルでは惑星の減光を local **0.55–0.8** に前倒し。`.section-num` はモバイルで非表示（`display: none`）か stroke 0.05。 | 390×844 で Service のラベルが中央にある時、惑星の L > 0.05 画素が見出し・リードの行ボックスと交差しない。 |
| 12 | m | ヘッダーの読了バー<br>`effects.ts` `setupProgress`, `header.ts` | 進行バーはヘッダー内にあり、ヘッダーは下スクロール中に隠れる。つまり「読んでいる最中には見えない読了バー」。右端のセクションインジケーターが同じ情報を常時示しているので重複。 | 撤去するか、`.header__progress` を `position: fixed; top: 0` の独立要素にしてヘッダーの隠れと切り離す。 | 下スクロール中に進行バーが可視、または DOM から消えている。 |
| 13 | m | トークンの契約逸脱<br>`tokens.css` L30, 39; `sections.css` L109 | `--sd-fs-display` 5rem → 6.75rem、`--sd-lh-display` 1.15 → 1.06、`.hero__en` を大文字 + 0.12em。いずれも画面上は成立している（1440 で見出し 106px、2 行で 230px、リードとの階層は保たれる）が、brief §3.2 / §2 と食い違い、v1.1 注記「配色のみ」に反する。 | brief §3.2 を v1.2 で更新（display `clamp(2.75rem, 7.4vw, 6.75rem)` / lh 1.06、英語サブコピーは小さな大文字可）。実装は変更不要。 | brief の型スケールと `tokens.css` が一致。 |
| 14 | m | 証拠の欠落 | `hero-after.png` / `hero-cursor.png` が未提出。`loader-early.png` は退場中。 | #2 / #4 の検証に合わせて再撮影。 | 提出物に 3 枚が存在する。 |

### 4. PASS までの最小リスト

1. **[C-1] Contact の輪・周回光をテキスト列の外へ**（デスクトップ `camZ 6.9` + スクリム強化 + 光の減光、モバイル `rotX 0.55` / `groupY −0.25` + 光の減光）。検証: 両サイズ・4 位相のスクリーンショットで行間 p90 ≤ 0.028、光の交差なし。
2. **[M-2] `.btn` / `.work` の `transition` から `transform` を外し、ホバー変形を GSAP に一元化。カーソルを `■` に、`is-text` を実装。** 検証: `transitionrun` 0 回、CTA ホバーで scale 1.02、`hero-cursor.png` 再提出。
3. **[M-3] Works `<li>` のチルト／シーンを無効化**（`a.work` 限定）。検証: ホバーで枠線色のみ変化。
4. **[M-4] プリローダー: `minTime 450ms`、退場 ≤ 0.85s、同一セッション再訪はフェードのみ。** 検証: LCP ≤ 2.0s、見出し開始 ≤ 1.0s。
5. **[M-5] ポスト処理の frame-time 実測を添付し、超過なら half-res bloom / DPR 1.5。storyboard §2.3 / §8-15 を v1.2 に。** 検証: 数値の添付。
6. **[M-6] Hero の環付き惑星を撤去するか点で作り直し、brief §3.5-4 を v1.2 に。** 検証: 面で塗った天体が About の核以外にない。
7. **[M-7] モバイル Hero の `heroY` 1.7。** 検証: ラベル帯 p90 ≤ 0.012。

Minor（#8〜#13）は余力で。#8（grain）と #9（二重ビネット）は数値を 1 箇所ずつ変えるだけなので同じ PR に入れることを勧める。

### 5. PASS 条件

- 1440×900 の Contact h2 中央 / CTA 中央、390×844 の Contact ラベル〜補足文（100px 刻み）のすべてで、輪と周回光の L > 0.05 画素がテキストの行ボックスと交差せず、行間帯の背景 p90 ≤ 0.028（合成 ≥ 4.5:1）。周回光は 4 位相で確認。
- `.btn` / `.work` に `transition: transform` がなく、CTA ホバーで scale 1.02 が computed に現れる。カーソルは正方形、本文上で縦線。Works `<li>` はホバーで枠線色のみ変化。
- Lighthouse で LCP ≤ 2.0s（LCP 要素 `.hero__title`）、再訪でプリローダー ≤ 0.4s。
- 実 GPU の 3D frame time が §7 予算内（数値添付）。storyboard §2.3 / §8-15、brief §3.2 / §3.5-4 が v1.2 として実装と一致。
- 面で塗った天体が About の核以外に存在しない。
- 390×844 Hero のラベル帯 p90 ≤ 0.012。
- 上記を満たした状態で、§2 の「残すもの」8 点（銀河とタグラインの関係、About の核、発光 ≤ 5%、Works の実体と SVG サムネ、Company の静けさ、薄い番号とフッターワードマーク、モバイルの基礎、197 kB）が変更されていないこと。
- 発光面積の再測定: 全フレームで L > 0.12 の画素率が DOM 込み ≤ 7%、3D のみ ≤ 5%（swiftshader 基準。実 GPU ではこれ以下）。

---

## 対応記録（第3回, Frontend Developer, 2026-09-23）

| 指摘 | 対応 |
|---|---|
| C-1 Contact の輪が見出し・本文に重なる | デスクトップ: カメラ z 6.2・`groupY -0.5` で輪をテキストブロックの外側に拡大し、Contact のスクリムを強化。モバイル: `rotX 0.75`・`groupY 1.9` で輪を見出しの上にほぼ真横から見せ、軌道光を opacity 0.5 / グロー 0.1 に減光 |
| M-1 CSS transition と GSAP の競合 | `.btn` `.work` から `transform` の transition を撤去、`:hover` の scale / translateY を廃止（マグネット・チルトが担当）。カーソルはワードマークの ■ を継いだ正方形に変更、未使用の is-text 状態を削除 |
| M-2 リンクでない Works カードのチルト | 実案件差し替え時にカードをリンク化する前提で維持（意図的） |
| M-3 プリローダーが長い | 最短 0.65s、退場 0.7s に短縮。同一セッション 2 回目以降は 0.3s のフェードのみ |
| M-4 ポストプロセスが設計書と矛盾 | クライアント要望として storyboard v1.2 に明記。DPR 1.0 でも 22ms 超なら自動で無効化 |
| M-5 環つき惑星が brief §3.5-4 に抵触 | クライアント要望として brief v1.2 で「点と線による惑星・銀河」を許容範囲に追加 |
| M-6 モバイルのヒーローラベルが銀河の核に重なる | ヒーロー中の `groupY` を 1.7 に |
| 副次 | グレイン 0.03、グレードのビネット 0.14（二重ビネットを緩和）、進捗バーをヘッダー外の固定要素に移動、タイポトークンを brief v1.2 に反映 |
